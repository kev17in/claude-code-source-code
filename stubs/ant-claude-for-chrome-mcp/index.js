/**
 * Stub for internal @ant/claude-for-chrome-mcp (not published for this research tree).
 * Chrome MCP paths are inert; real installs use the official npm package binary.
 */
export const BROWSER_TOOLS = []

export function createClaudeForChromeMcpServer(_context) {
  return {
    async connect(_transport) {},
  }
}
